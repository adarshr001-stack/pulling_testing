# SFTP Configuration
BANK_HOST=your-sftp-server.example.com
BANK_PORT=22
BANK_USERNAME=your-username

# Private Key (use ONE of the following methods)
# Method 1: Path to private key file
BANK_PRIVATE_KEY_PATH=/path/to/your/private_key.pem

# Method 2: Private key content (PEM format inline)
# BANK_PRIVATE_KEY_CONTENT=-----BEGIN RSA PRIVATE KEY----- ... -----END RSA PRIVATE KEY-----

# Remote path to sync (directory or specific file)
BANK_REMOTE_PATH=/remote/path/to/files

# S3-Compatible Storage Configuration
STORAGE_ENDPOINT=https://s3.amazonaws.com
# Or for MinIO/Ceph: http://localhost:9000
STORAGE_ACCESS_KEY=your-access-key
STORAGE_SECRET_KEY=your-secret-key
STORAGE_BUCKET=your-bucket-name
STORAGE_REGION=us-east-1

# Performance Configuration
UPLOAD_CHUNK_SIZE_MB=10          # Chunk size for multipart uploads (default: 10)
MAX_CONCURRENT_UPLOADS=4         # Number of parallel workers (default: 4)
